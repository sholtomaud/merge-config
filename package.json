{
  "name": "merge-config",
  "version": "1.0.6",
  "description": "A lib to merge items provided on command line and config.json file.",
  "main": "src/index.js",
  "scripts": {
    "test": "tape './test/**/*.test.js'",
    "tpl": "npm test && npm link",
    "patch": "npm version patch --no-git-tag-version && git add . && git commit -m 'Bump patch version'",
    "pr": "gh pr create --base main --head install -y",
    "tppr": "npm run test && npm run patch && npm run pr"
  },
  "devDependencies": {
    "minimist": "^1.2.8",
    "tape": "^5.3.2"
  },
  "keywords": [
    "config",
    "merge",
    "command-line",
    "json"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.12.0"
  }
}
