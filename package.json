{
  "name": "merge-config",
  "version": "1.0.4",
  "description": "A lib to merge items provided on command line and config.json file.",
  "main": "src/index.js",
  "scripts": {
    "test": "tape './test/**/*.test.js'",
    "tpl": "npm test && npm version patch && npm link"
  },
  "devDependencies": {
    "minimist": "^1.2.8",
    "tape": "^5.3.2"
  },
  "keywords": [
    "config",
    "merge",
    "command-line",
    "json"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "ajv": "^8.12.0"
  }
}
